pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
                withMaven(maven : 'Apache Maven 3.6.2') {
                    sh 'mvn install'
                }
            }
        }
    }
}